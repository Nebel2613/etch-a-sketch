const pixel = document.createElement("div");
const btn = document.querySelector("button");
btn.addEventListener("click", ()=>{
    const INPUT = parseInt(prompt("How many pixels is there to be on the side (Ex: 16 for a 16x16 grid)", "16"));
    
})